CONFIG_I2C=y
CONFIG_UART_INTERRUPT_DRIVEN=y
CONFIG_GPIO=y
# Explicitly disable MAX31790 drivers — bridge test uses raw I2C only;
# no driver init packets should appear in the test output.
CONFIG_MFD_MAX31790=n
CONFIG_PWM_MAX31790=n
CONFIG_MAX31790_FAN_SPEED=n
CONFIG_MAX31790_FAN_FAULT=n
